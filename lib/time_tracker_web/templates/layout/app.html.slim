doctype html
html lang="en" 
  head
    meta content=("text/html; charset=UTF-8") http-equiv="Content-Type" /
    meta charset="utf-8" /
    meta content="IE=edge" http-equiv="X-UA-Compatible" /
    meta content="width=device-width, initial-scale=1.0" name="viewport" /
    title TimeTracker · Phoenix Framework
     <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700&display=swap" rel="stylesheet">
    link href="#{Routes.static_path(@conn, "/css/app.css")}" rel="stylesheet" /
    script defer="defer" src="#{Routes.static_path(@conn, "/js/app.js")}" type="text/javascript" 
  body
    = render SharedView, "_navbar.html", assigns
    main.container role="main" 
      = if info = get_flash(@conn, :info) do
        p.notification.is-info role="alert" = info
      = if error = get_flash(@conn, :error) do
        p.notification.is-danger role="alert" = error
      = @inner_content
